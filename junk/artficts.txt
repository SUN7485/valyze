import { useState, useRef, useCallback } from "react";
import * as mammoth from "mammoth";

const SYSTEM_PROMPT = `You are a senior credit intelligence analyst for the Valyze Credit Intelligence Platform.

═══════════════════════════════════════════════
CORE SYSTEM RULES — PERMANENT — APPLY TO EVERY REPORT
═══════════════════════════════════════════════

RULE 1 — DATA INTEGRITY
Prioritize source documents. For any missing field, apply this resolution order:
1. Extract from uploaded documents
2. Search the web (use your 3 allowed searches wisely)
3. Calculate or derive from other available data (e.g. industry codes from SIC→NACE crosswalk, market size from industry reports, ratios from financial data)
4. Use industry standard defaults or benchmarks for fields like analyst info, review dates, utilization estimates
5. Only as a last resort: null for numeric fields, "N/A" for text fields

NEVER use "Not disclosed in source documents" — this is no longer valid output.
For fields that genuinely cannot be found or calculated (e.g. internal client references, private analyst IDs), use "N/A".
For operational estimates (local purchasing %, markets count, etc.) use your best estimate based on industry context and flag in data_limitations.
Never invent specific registration numbers, certificate numbers, or legal identifiers — use "N/A" for those only.

RULE 2 — AUDITOR ATTRIBUTION
For each financial year, identify the auditor by reading the signature/opinion page of THAT specific year's audit report independently. Do not assume continuity between years. Each year's auditor is only the firm that signed that specific year's audit opinion.

RULE 3 — EQUITY IDENTIFICATION
Scan the FULL equity section of the balance sheet and identify ALL components of owner-provided funds including: share capital, retained earnings, statutory reserves, revaluation reserves, shareholder current accounts, owner contribution accounts, and any quasi-equity instruments. Sum ALL components as the equity base. Do not stop at the first subtotal. If multiple subtotals exist within equity, always use the final total that includes all components.

RULE 4 — EBITDA CALCULATION
Always build EBITDA from verified components in the source documents:
EBITDA = Net profit before tax + Finance costs + ALL depreciation components + ALL amortisation components
Depreciation components must be sourced from the notes to financial statements, not just the face of the P&L. Check every depreciation-related note (ROU assets, investment property, intangibles, deferred costs) before finalising EBITDA. Missing one component cascades into wrong EBITDA margin and wrong interest coverage.

RULE 5 — SOURCE CONFLICT RESOLUTION
Priority order when sources conflict:
1. Audited financial statements
2. Notes to audited financial statements
3. Auditor-signed supplementary schedules
4. Company-provided questionnaire
5. Management commentary or projections
Always use the higher-priority value and log conflicts in data_limitations.

RULE 6 — FIELD CONSISTENCY
These field pairs must always be identical:
- "credit_rating" = "final_credit_rating"
- "recommended_limit" = "recommended_credit_limit"
- "max_exposure" = "maximum_exposure"
Generate one value and copy it exactly to both. Never let paired fields differ.

RULE 7 — DATA TYPES
Every value in the JSON must be a string. No exceptions.
✅ "field": "72"   ✅ "field": "3.60"   ❌ "field": 72   ❌ "field": 3.60

RULE 8 — STATUS FIELD VALUES
The status field for every ratio must only contain one of these three exact strings:
"success" → ratio meets or exceeds benchmark
"warning" → ratio below benchmark or declining
"danger"  → ratio at critical or distressed level
No other values are valid. The human-readable description belongs in the label field, not the status field.

RULE 9 — ANALYST BENCHMARKS
Industry benchmark values are set by the analyst. Do not recalculate, adjust, or override them. Use provided benchmarks exactly as given.

RULE 10 — CONTACT INFORMATION PRIORITY
Use contact information from official company documents (audited financial statement letterhead, registration documents, verified website) over questionnaire-provided contacts. Log any discrepancy in data_limitations.

═══════════════════════════════════════════════
SPEED RULES — CRITICAL:
- Perform a MAXIMUM of 3 web searches. Only search for the single most important missing piece (e.g., company registry, recent news). Stop searching after 3.
- Prioritize uploaded documents above all else.
- Do not search for things you can infer from the documents.

OUTPUT: Return ONLY a single valid JSON object. No markdown, no code blocks, no explanations. Start immediately with { and end with }.

The JSON must contain ALL of these flat keys matching the HTML template variables exactly:

REPORT META: report_id (generate as VCR-YYYYMMDD-XXXX), report_date (today), current_year, client_name, client_reference, analyst_name, analyst_id, analyst_department, analyst_email, analyst_phone, qa_reviewer_name, qa_review_date

ORDER: order_comment

COMPANY IDENTITY: company_name (= legal_name), legal_name, trade_names, cr_number, unified_number, investment_license_no, license_type, issue_date, expiry_date, capital, company_type, company_duration, company_status, company_status_badge (low/medium/high), status_badge (low/medium/high), incorporation_date, incorporation_state, country, city, company_address, headquarters_address, phone, fax, email, website, auditor_name, sic_codes, industry, employee_count

COUNTRY FIELDS: show_egypt_fields (bool), tax_registration_number, tax_card_number, trade_license_number, social_insurance_number, gafi_registration, show_saudi_fields (bool), zakat_certificate, vat_registration_number, gosi_registration, nitaqat_band, municipality_license, show_uae_fields (bool), trn_vat, ded_number, freezone_license
extra_reg_fields: [{extra_reg_label, extra_reg_value}]

EXECUTIVE SUMMARY: executive_summary_text (max 120 words), company_history_text (max 80 words), exec_current_ratio, exec_equity_ratio, exec_profitability

OWNERSHIP: parent_company, subsidiaries, affiliates, ultimate_beneficial_owner
shareholders: [{name, position, percentage, nationality, type}]

MANAGEMENT: management_team: [{name, title, department, contact_phone, contact_email, bio}]
show_board_of_directors (bool), board_members: [{name, role, nationality, since, bio_short}]

RELATED CONCERNS: show_related_concerns (bool), group_hq_name, group_hq_location
branches: [{branch_name, branch_unified_no, branch_cr_no, branch_city, branch_function, branch_status, branch_status_badge}]
regional_affiliates: [{affiliate_name}]

OPERATIONS: registration_activities_description, activities_full_description, nace_codes, nace_description, hs_codes, hs_description, employee_location, facilities_count, main_facility_location, markets_count, markets_regions

SUPPLY CHAIN — PURCHASING: main_suppliers, local_purchasing_pct, local_purchasing_detail, import_purchasing_pct, import_countries, import_items, supplier_payment_method, supplier_payment_terms
SUPPLY CHAIN — SALES: key_customers, local_sales_pct, local_sales_detail, export_sales_pct, export_countries, export_items, customer_payment_method, customer_payment_terms

BANKING: banking_relationships: [{bank_name, facility_type, facility_usage}], total_banks, primary_bank, group_treasury_support, banking_notes

FINANCIALS STATUS: fin_currency, fin_unit_scale, fin_statement_type, fin_period_end, fin_scope, fin_ratio_basis, fin_parent_note
cash_flow_operating_1, cash_flow_operating_2, cash_flow_operating_3, cash_flow_operating_trend
cash_flow_investing_1, cash_flow_investing_2, cash_flow_investing_3, cash_flow_investing_trend
cash_flow_financing_1, cash_flow_financing_2, cash_flow_financing_3, cash_flow_financing_trend
cash_flow_end_1, cash_flow_end_2, cash_flow_end_3, cash_flow_end_trend

FINANCIALS (use LOCAL currency, comma-formatted strings for display):
year_1 (label e.g. "2024"), year_2, year_3
revenue_1, revenue_2, revenue_3, revenue_trend (e.g. "↑ Growing +12%")
cogs_1, cogs_2, cogs_3, cogs_trend
gross_profit_1, gross_profit_2, gross_profit_3, gross_profit_trend
opex_1, opex_2, opex_3, opex_trend
ebitda_1, ebitda_2, ebitda_3, ebitda_trend
net_income_1, net_income_2, net_income_3, net_income_trend
cash_1, cash_2, cash_3, cash_trend
ar_1, ar_2, ar_3, ar_trend
inventory_1, inventory_2, inventory_3, inventory_trend
current_assets_1, current_assets_2, current_assets_3, current_assets_trend
total_assets_1, total_assets_2, total_assets_3, total_assets_trend
current_liabilities_1, current_liabilities_2, current_liabilities_3, current_liabilities_trend
ltd_1, ltd_2, ltd_3, ltd_trend
total_liabilities_1, total_liabilities_2, total_liabilities_3, total_liabilities_trend
equity_1, equity_2, equity_3, equity_trend

RATIOS (numeric values + industry benchmarks):
current_ratio, current_ratio_prev, current_ratio_industry, current_ratio_status, current_ratio_label, current_ratio_interpretation
quick_ratio, quick_ratio_prev, quick_ratio_industry, quick_ratio_status, quick_ratio_label, quick_ratio_interpretation
cash_ratio, cash_ratio_prev, cash_ratio_industry, cash_ratio_status, cash_ratio_label, cash_ratio_interpretation
gross_margin, gross_margin_prev, gross_margin_industry, gross_margin_status, gross_margin_label, gross_margin_interpretation
ebitda_margin, ebitda_margin_prev, ebitda_margin_industry, ebitda_margin_status, ebitda_margin_label, ebitda_margin_interpretation
net_margin, net_margin_prev, net_margin_industry, net_margin_status, net_margin_label, net_margin_interpretation
roa, roa_prev, roa_industry, roa_status, roa_label, roa_interpretation
roe, roe_prev, roe_industry, roe_status, roe_label, roe_interpretation
debt_equity, debt_equity_prev, debt_equity_industry, debt_equity_status, debt_equity_label, debt_equity_interpretation
debt_assets, debt_assets_prev, debt_assets_industry, debt_assets_status, debt_assets_label, debt_assets_interpretation
equity_ratio, equity_ratio_prev, equity_ratio_industry, equity_ratio_status, equity_ratio_label, equity_ratio_interpretation
interest_coverage, interest_coverage_prev, interest_coverage_industry, interest_coverage_status, interest_coverage_label, interest_coverage_interpretation
asset_turnover, asset_turnover_prev, asset_turnover_industry, asset_turnover_status, asset_turnover_label, asset_turnover_interpretation
dio, dio_prev, dio_industry, dio_status, dio_label, dio_interpretation
dso, dso_prev, dso_industry, dso_status, dso_label, dso_interpretation
dpo, dpo_prev, dpo_industry, dpo_status, dpo_label, dpo_interpretation
ccc, ccc_prev, ccc_industry, ccc_status, ccc_label, ccc_interpretation
ebit_margin (numeric, for dashboard display)

RISK SCORES (0–100):
credit_rating (AAA/AA/A/BBB/BB+/B/CCC), rating_color (green/yellow/red), risk_level (Low/Medium/High/Critical), risk_color (green/yellow/red)
health_score, viability_score, viability_color (green/yellow/red), viability_level, viability_badge (low/medium/high), viability_probability, viability_meaning
delinquency_score, delinquency_color, delinquency_level, delinquency_badge, delinquency_probability, delinquency_meaning
failure_score, failure_color, payment_score, payment_color, paydex_score
company_size, annual_revenue, payment_risk, financial_health
recommended_limit (USD), max_exposure (USD)

DASHBOARD: alerts: [{alert_type (success/warning/danger/info), alert_icon, alert_message}]

PAYMENT BEHAVIOR: avg_dbt, pct_on_time, highest_past_due, prompt_pct, prompt_amount, slow_30_pct, slow_30_amount, slow_60_pct, slow_60_amount, slow_90plus_pct, slow_90plus_amount

LEGAL: lawsuit_count, lawsuit_amount, lawsuit_last_date, lawsuit_status, lawsuit_badge (low/medium/high), lien_count, lien_amount, lien_last_date, lien_status, lien_badge, judgment_count, judgment_amount, judgment_last_date, judgment_status, judgment_badge, license_alert (success/warning/danger), license_icon, license_status, license_expiry, tax_alert, tax_icon, tax_status
legal_details: [{event_type, event_date, event_amount, event_description}]

NEWS (2024+ ONLY): news_events: [{event_date, event_title, event_summary, event_sentiment (positive/negative/neutral), event_sentiment_label}]

INDUSTRY: industry_name, market_size, industry_growth_rate
sector_country_label, sector_year, sector_market_size, sector_market_size_comment, sector_forecast_period, sector_growth_forecast, sector_growth_comment, sector_local_share, sector_local_comment, sector_trade_flow, sector_trade_comment, sector_risks, sector_drivers, sector_major_players, sector_summary_text
strengths: [], weaknesses: [], opportunities: [], threats: [] (max 4 per array, credit/payment focused)

CREDIT RECOMMENDATION (all limits in USD): final_credit_rating, final_risk_level, final_risk_color, recommended_credit_limit, maximum_exposure, recommended_payment_terms, review_frequency, credit_opinion_text (decisive: Approve / Approve with Conditions / Decline)

MONITORING: monitoring_triggers: [{trigger_event, trigger_action}], payment_delay_status, payment_delay_color, credit_utilization, utilization_color, financial_trend, trend_color, legal_threshold, payment_delay_threshold, next_review_date, assigned_analyst, escalation_contact

DATA QUALITY: data_quality_rating, data_limitations, data_source_analyst_comment

NUMBER RULES:
- financial_data display fields: comma-formatted strings (e.g. "12,450,000")  
- ratio fields: numeric only (e.g. 1.45)
- Trend fields: use arrows (↑ Growing +X% / ↓ Declining X% / → Stable)
- All credit limits/exposures: USD
- Financial statements: company's local currency
- News: 2024+ only`;

const STAGES = ["Extracting docs", "Web search (max 3)", "Calculating ratios", "Building JSON"];
const ACCEPT = ".pdf,image/*,.xlsx,.csv,.txt,.docx,.doc,.png,.jpg,.jpeg,.webp,.gif";
const fIcon = f => f.type === "application/pdf" ? "📄" : f.type?.startsWith("image/") ? "🖼️" : f.name.match(/\.docx?$/i) ? "📝" : "📊";

export default function ValyzeExtractor() {
  const [files, setFiles] = useState([]);
  const [status, setStatus] = useState("idle");
  const [stage, setStage] = useState(0);
  const [result, setResult] = useState(null);
  const [error, setError] = useState("");
  const [tab, setTab] = useState("summary");
  const [copied, setCopied] = useState(false);
  const [patchJSON, setPatchJSON] = useState("");
  const [patchInstructions, setPatchInstructions] = useState("");
  const [patchStatus, setPatchStatus] = useState("idle");
  const [patchError, setPatchError] = useState("");
  const fileRef = useRef();

  const addFiles = f => setFiles(p => [...p, ...[...f]].slice(0, 5));
  const onDrop = useCallback(e => { e.preventDefault(); addFiles(e.dataTransfer.files); }, []);
  const toB64 = f => new Promise((res, rej) => { const r = new FileReader(); r.onload = () => res(r.result.split(",")[1]); r.onerror = rej; r.readAsDataURL(f); });

  const extract = async () => {
    if (!files.length) return;
    setStatus("loading"); setStage(0); setResult(null); setError("");
    try {
      const blocks = [];
      for (const f of files) {
        if (f.type === "application/pdf") blocks.push({ type: "document", source: { type: "base64", media_type: "application/pdf", data: await toB64(f) } });
        else if (f.type?.startsWith("image/") || f.name.match(/\.(png|jpg|jpeg|webp|gif)$/i)) blocks.push({ type: "image", source: { type: "base64", media_type: f.type || "image/jpeg", data: await toB64(f) } });
        else if (f.name.match(/\.docx?$/i)) { const ab = await f.arrayBuffer(); const { value } = await mammoth.extractRawText({ arrayBuffer: ab }); blocks.push({ type: "text", text: `[Word: ${f.name}]\n\n${value}` }); }
        else blocks.push({ type: "text", text: `[File: ${f.name}]\n\n${await f.text().catch(() => "n/a")}` });
      }
      blocks.push({ type: "text", text: "Extract all fields from the documents. Use web_search AT MOST 3 times for the most critical missing data only. Return the complete flat JSON object matching all template variables." });

      const timer = setInterval(() => setStage(s => s < 3 ? s + 1 : s), 8000);
      const msgs = [{ role: "user", content: blocks }];
      let finalText = "";

      for (let i = 0; i < 8; i++) {
        const res = await fetch("https://api.anthropic.com/v1/messages", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ model: "claude-sonnet-4-20250514", max_tokens: 16000, system: SYSTEM_PROMPT, tools: [{ type: "web_search_20250305", name: "web_search" }], messages: msgs })
        });
        if (!res.ok) { const e = await res.json(); throw new Error(e?.error?.message || "API error"); }
        const data = await res.json();
        const txt = data.content.filter(b => b.type === "text").map(b => b.text).join("");
        if (txt) finalText = txt;
        if (data.stop_reason === "end_turn") break;
        if (data.stop_reason === "tool_use") {
          msgs.push({ role: "assistant", content: data.content });
          msgs.push({ role: "user", content: data.content.filter(b => b.type === "tool_use").map(b => ({ type: "tool_result", tool_use_id: b.id, content: JSON.stringify(b.input) })) });
          setStage(1);
        } else break;
      }
      clearInterval(timer); setStage(3);
      const m = finalText.match(/\{[\s\S]*\}/);
      if (!m) throw new Error("No valid JSON returned. Try again.");
      setResult(JSON.parse(m[0]));
      setStatus("done");
    } catch (e) { setError(e.message); setStatus("error"); }
  };

  const downloadJSON = () => {
    try {
      const json = JSON.stringify(result, null, 2);
      const name = `valyze_${(result?.company_name || "report").replace(/[^a-z0-9]/gi, "_")}.json`;
      const uri = "data:application/octet-stream;charset=utf-8," + encodeURIComponent(json);
      const a = document.createElement("a");
      a.href = uri;
      a.download = name;
      a.style.display = "none";
      document.body.appendChild(a);
      a.click();
      setTimeout(() => document.body.removeChild(a), 500);
    } catch (e) { alert("Download failed: " + e.message); }
  };

  const copyJSON = () => {
    const json = JSON.stringify(result, null, 2);
    try {
      if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(json).then(() => { setCopied(true); setTimeout(() => setCopied(false), 2000); }).catch(() => fallbackCopy(json));
      } else { fallbackCopy(json); }
    } catch { fallbackCopy(json); }
  };

  const fallbackCopy = (text) => {
    const ta = document.createElement("textarea");
    ta.value = text;
    ta.style.cssText = "position:fixed;top:0;left:0;opacity:0;";
    document.body.appendChild(ta);
    ta.focus(); ta.select();
    try { document.execCommand("copy"); setCopied(true); setTimeout(() => setCopied(false), 2000); } catch {}
    document.body.removeChild(ta);
  };

  const d = result || {};
  const Badge = ({ v }) => { const c = { Low: "#22c55e", Medium: "#f59e0b", High: "#ef4444", Critical: "#7c3aed", green: "#22c55e", yellow: "#f59e0b", red: "#ef4444" }; return v ? <span style={{ background: c[v] || "#6b7280", color: "#fff", borderRadius: 6, padding: "2px 10px", fontSize: 12, fontWeight: 700 }}>{v}</span> : <span style={{ color: "#475569" }}>—</span>; };
  const Bar = ({ label, val, invert }) => { const w = Math.max(0, Math.min(100, invert ? 100 - (val || 0) : (val || 0))); const bg = w > 70 ? "#22c55e" : w > 40 ? "#f59e0b" : "#ef4444"; return <div style={{ marginBottom: 10 }}><div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginBottom: 3 }}><span style={{ color: "#94a3b8" }}>{label}</span><span style={{ color: "#f1f5f9", fontWeight: 600 }}>{val ?? "—"}</span></div><div style={{ background: "#1e293b", borderRadius: 4, height: 6 }}><div style={{ width: `${w}%`, background: bg, height: 6, borderRadius: 4, transition: "width 0.8s" }} /></div></div>; };
  const Sec = ({ title, ch }) => <div style={{ background: "#0f172a", border: "1px solid #1e293b", borderRadius: 10, padding: 18, marginBottom: 14 }}><div style={{ color: "#64748b", fontSize: 11, fontWeight: 700, textTransform: "uppercase", letterSpacing: 1, marginBottom: 12 }}>{title}</div>{ch}</div>;
  const F = ({ l, v }) => <div style={{ display: "flex", justifyContent: "space-between", padding: "5px 0", borderBottom: "1px solid #0d1526", fontSize: 13 }}><span style={{ color: "#64748b", flexShrink: 0, marginRight: 8 }}>{l}</span><span style={{ color: "#e2e8f0", fontWeight: 500, maxWidth: "62%", textAlign: "right" }}>{v ?? "—"}</span></div>;
  const runPatch = async () => {
    if (!patchJSON.trim() || !patchInstructions.trim()) return;
    setPatchStatus("loading"); setPatchError("");
    try {
      let parsed;
      try { parsed = JSON.parse(patchJSON); } catch { throw new Error("Invalid JSON — please check your pasted JSON."); }
      const res = await fetch("https://api.anthropic.com/v1/messages", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
                  body: JSON.stringify({
          model: "claude-haiku-4-5-20251001",
          max_tokens: 16000,
          system: `You are a JSON patch engine. The user will give you a JSON object and a list of changes to apply.
RULES:
- Apply ONLY the changes listed by the user
- DO NOT rename any existing fields
- DO NOT reorder sections unless told to
- DO NOT add new fields unless the user explicitly says "add new field" or "add"
- Return ONLY the complete updated JSON object, no markdown, no explanations, no code blocks
- Start immediately with { and end with }`,
          messages: [{ role: "user", content: `Here is the full JSON:\n${JSON.stringify(parsed, null, 2)}\n\n## CHANGES TO APPLY:\n${patchInstructions}\n\nReturn only the complete patched JSON.` }]
        })
      });
      if (!res.ok) { const e = await res.json(); throw new Error(e?.error?.message || "API error"); }
      const data = await res.json();
      const raw = data.content.filter(b => b.type === "text").map(b => b.text).join("");
      const m = raw.match(/\{[\s\S]*\}/);
      if (!m) throw new Error("No valid JSON returned.");
      const fixed = JSON.parse(m[0]);
      setResult(fixed); setStatus("done"); setTab("json");
      setPatchStatus("done");
    } catch (e) { setPatchError(e.message); setPatchStatus("error"); }
  };

  const TABS = ["summary", "operations", "financials", "ratios", "risk", "legal", "news", "recommendation", "json", "patch"];

  return (
    <div style={{ background: "#020817", minHeight: "100vh", fontFamily: "'Inter',system-ui,sans-serif", color: "#e2e8f0" }}>
      <div style={{ borderBottom: "1px solid #1e293b", padding: "14px 20px", display: "flex", alignItems: "center", gap: 12 }}>
        <div style={{ background: "linear-gradient(135deg,#3b82f6,#8b5cf6)", borderRadius: 8, width: 30, height: 30, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 15 }}>⚡</div>
        <div>
          <div style={{ fontWeight: 700, fontSize: 14 }}>Valyze Credit Intelligence v4</div>
          <div style={{ color: "#475569", fontSize: 11 }}>Template-Matched · Max 3 Web Searches · Fast Mode</div>
        </div>
      </div>

      <div style={{ maxWidth: 960, margin: "0 auto", padding: "20px 16px" }}>
        {status !== "done" && (
          <>
            <div onDragOver={e => e.preventDefault()} onDrop={onDrop} onClick={() => fileRef.current.click()}
              style={{ border: "2px dashed #1e40af", borderRadius: 12, padding: "36px 24px", textAlign: "center", cursor: "pointer", background: "#0f172a", marginBottom: 12 }}>
              <input ref={fileRef} type="file" multiple accept={ACCEPT} style={{ display: "none" }} onChange={e => addFiles(e.target.files)} />
              <div style={{ fontSize: 32, marginBottom: 8 }}>📂</div>
              <div style={{ fontWeight: 600, marginBottom: 4 }}>Drop company documents here</div>
              <div style={{ color: "#475569", fontSize: 13 }}>PDF · Word · Images · Excel · CSV · TXT — up to 5 files</div>
            </div>
            {files.map((f, i) => (
              <div key={i} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", background: "#0f172a", border: "1px solid #1e293b", borderRadius: 8, padding: "9px 14px", marginBottom: 6 }}>
                <span style={{ fontSize: 13, color: "#cbd5e1" }}>{fIcon(f)} {f.name} <span style={{ color: "#475569" }}>({(f.size / 1024).toFixed(0)} KB)</span></span>
                <button onClick={() => setFiles(p => p.filter((_, j) => j !== i))} style={{ background: "none", border: "none", color: "#ef4444", cursor: "pointer", fontSize: 16 }}>✕</button>
              </div>
            ))}
          </>
        )}

        {status === "loading" && (
          <div style={{ background: "#0f172a", border: "1px solid #1e293b", borderRadius: 12, padding: 28, textAlign: "center", marginBottom: 16 }}>
            <div style={{ fontSize: 28, marginBottom: 12 }}>⚙️</div>
            <div style={{ fontWeight: 600, marginBottom: 16 }}>Running fast extraction pipeline...</div>
            <div style={{ display: "flex", gap: 8, justifyContent: "center", flexWrap: "wrap" }}>
              {STAGES.map((s, i) => (
                <div key={i} style={{ padding: "5px 12px", borderRadius: 20, fontSize: 12, fontWeight: 600, background: i <= stage ? "linear-gradient(135deg,#3b82f6,#8b5cf6)" : "#1e293b", color: i <= stage ? "#fff" : "#475569", transition: "all 0.4s" }}>
                  {i < stage ? "✓" : i === stage ? "◐" : "○"} {s}
                </div>
              ))}
            </div>
          </div>
        )}

        {status === "error" && <div style={{ background: "#1a0a0a", border: "1px solid #7f1d1d", borderRadius: 10, padding: 14, marginBottom: 16, color: "#fca5a5", fontSize: 13 }}>⚠️ {error}</div>}

        {status !== "done" && (
          <button onClick={extract} disabled={!files.length || status === "loading"}
            style={{ width: "100%", padding: 13, borderRadius: 10, border: "none", background: !files.length || status === "loading" ? "#1e293b" : "linear-gradient(135deg,#3b82f6,#8b5cf6)", color: !files.length || status === "loading" ? "#475569" : "#fff", fontWeight: 700, fontSize: 15, cursor: !files.length || status === "loading" ? "default" : "pointer" }}>
            {status === "loading" ? "Extracting..." : "⚡ Run Credit Intelligence Extraction"}
          </button>
        )}

        {status === "done" && result && (
          <div>
            {/* Tab bar */}
            <div style={{ display: "flex", gap: 5, flexWrap: "wrap", marginBottom: 12 }}>
              {TABS.map(t => <button key={t} onClick={() => setTab(t)} style={{ padding: "5px 11px", borderRadius: 8, border: "none", background: tab === t ? "#3b82f6" : "#1e293b", color: tab === t ? "#fff" : "#94a3b8", fontWeight: 600, fontSize: 12, cursor: "pointer", textTransform: "capitalize" }}>{t === "json" ? "Raw JSON" : t}</button>)}
            </div>

            {/* Action buttons */}
            <div style={{ display: "flex", gap: 8, marginBottom: 16, flexWrap: "wrap", alignItems: "center" }}>
              <button onClick={copyJSON} style={{ padding: "7px 12px", borderRadius: 8, border: "1px solid #1e293b", background: "#0f172a", color: "#94a3b8", cursor: "pointer", fontSize: 13 }}>{copied ? "✓ Copied" : "Copy JSON"}</button>
              <button onClick={downloadJSON} style={{ padding: "7px 12px", borderRadius: 8, border: "none", background: "#22c55e", color: "#fff", fontWeight: 700, cursor: "pointer", fontSize: 13 }}>⬇ Download JSON</button>
              <button onClick={() => { setStatus("idle"); setFiles([]); setResult(null); }} style={{ padding: "7px 12px", borderRadius: 8, border: "1px solid #1e293b", background: "#0f172a", color: "#94a3b8", cursor: "pointer", fontSize: 13 }}>New Report</button>
            </div>

            {/* SUMMARY */}
            {tab === "summary" && <div>
              <Sec title="Company Identity" ch={<>
                <F l="Legal Name" v={d.legal_name} /><F l="Trade Names" v={d.trade_names} /><F l="CR / Reg No." v={d.cr_number} /><F l="Unified No." v={d.unified_number} />
                <F l="Country / City" v={`${d.country || "—"} / ${d.city || "—"}`} /><F l="Industry" v={d.industry} /><F l="Type / Status" v={`${d.company_type || "—"} / ${d.company_status || "—"}`} />
                <F l="Incorporated" v={d.incorporation_date} /><F l="Capital" v={d.capital} /><F l="Employees" v={d.employee_count} /><F l="Website" v={d.website} /><F l="Email" v={d.email} /><F l="Phone" v={d.phone} />
              </>} />
              <Sec title="Executive Summary" ch={<>
                <div style={{ color: "#cbd5e1", fontSize: 13, lineHeight: 1.7, marginBottom: 10 }}>{d.executive_summary_text || "—"}</div>
                <div style={{ color: "#94a3b8", fontSize: 12, fontStyle: "italic", lineHeight: 1.6 }}>{d.company_history_text}</div>
              </>} />
              <Sec title="Ownership" ch={<>
                <F l="UBO" v={d.ultimate_beneficial_owner} /><F l="Parent" v={d.parent_company} /><F l="Subsidiaries" v={d.subsidiaries} />
                {(d.shareholders || []).map((s, i) => <F key={i} l={`Shareholder ${i + 1}`} v={`${s.name} · ${s.percentage}% · ${s.nationality}`} />)}
              </>} />
              <Sec title="Management" ch={(d.management_team || []).map((m, i) => <div key={i} style={{ borderBottom: "1px solid #1e293b", padding: "8px 0" }}><div style={{ color: "#93c5fd", fontWeight: 600, fontSize: 13 }}>{m.name} <span style={{ color: "#64748b" }}>· {m.title}</span></div><div style={{ color: "#94a3b8", fontSize: 12, marginTop: 3 }}>{m.bio}</div></div>)} />
            </div>}

            {/* OPERATIONS */}
            {tab === "operations" && <div>
              <Sec title="Activities" ch={<div style={{ color: "#cbd5e1", fontSize: 13, lineHeight: 1.7 }}>{d.registration_activities_description || d.activities_full_description || "—"}</div>} />
              <Sec title="Classification" ch={<><F l="NACE" v={`${d.nace_codes || "—"} — ${d.nace_description || ""}`} /><F l="HS Codes" v={`${d.hs_codes || "—"} — ${d.hs_description || ""}`} /><F l="SIC" v={d.sic_codes} /></>} />
              <Sec title="Operations" ch={<><F l="Employees" v={`${d.employee_count || "—"} · ${d.employee_location || ""}`} /><F l="Facilities" v={`${d.facilities_count || "—"} · ${d.main_facility_location || ""}`} /><F l="Markets" v={`${d.markets_count || "—"} · ${d.markets_regions || ""}`} /></>} />
              <Sec title="Supply Chain — Purchasing" ch={<><F l="Main Suppliers" v={d.main_suppliers} /><F l="Local Sourcing" v={`${d.local_purchasing_pct || 0}% — ${d.local_purchasing_detail || ""}`} /><F l="Imports" v={`${d.import_purchasing_pct || 0}% from ${d.import_countries || "—"}`} /><F l="Items" v={d.import_items} /><F l="Payment" v={`${d.supplier_payment_method || "—"} · ${d.supplier_payment_terms || ""}`} /></>} />
              <Sec title="Supply Chain — Sales" ch={<><F l="Key Customers" v={d.key_customers} /><F l="Local Sales" v={`${d.local_sales_pct || 0}% — ${d.local_sales_detail || ""}`} /><F l="Exports" v={`${d.export_sales_pct || 0}% to ${d.export_countries || "—"}`} /><F l="Items" v={d.export_items} /><F l="Payment" v={`${d.customer_payment_method || "—"} · ${d.customer_payment_terms || ""}`} /></>} />
              <Sec title="Banking" ch={<><F l="Primary Bank" v={d.primary_bank} /><F l="Total Partners" v={d.total_banks} /><F l="Group Treasury" v={d.group_treasury_support} />{(d.banking_relationships || []).map((b, i) => <F key={i} l={b.bank_name || `Bank ${i+1}`} v={`${b.facility_type} · ${b.facility_usage}`} />)}</>} />
            </div>}

            {/* FINANCIALS */}
            {tab === "financials" && <div>
              <Sec title="Financial Statement Info" ch={<><F l="Currency" v={d.fin_currency} /><F l="Scale" v={d.fin_unit_scale} /><F l="Type" v={d.fin_statement_type} /><F l="Period End" v={d.fin_period_end} /></>} />
              <Sec title="Income Statement" ch={
                <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                  <thead><tr>{["Item", d.year_3 || "Y-2", d.year_2 || "Y-1", d.year_1 || "Latest", "Trend"].map(h => <th key={h} style={{ background: "#1e3a5f", color: "#93c5fd", padding: "6px 8px", textAlign: "left" }}>{h}</th>)}</tr></thead>
                  <tbody>{[["Revenue", d.revenue_3, d.revenue_2, d.revenue_1, d.revenue_trend], ["COGS", d.cogs_3, d.cogs_2, d.cogs_1, d.cogs_trend], ["Gross Profit", d.gross_profit_3, d.gross_profit_2, d.gross_profit_1, d.gross_profit_trend], ["OPEX", d.opex_3, d.opex_2, d.opex_1, d.opex_trend], ["EBITDA", d.ebitda_3, d.ebitda_2, d.ebitda_1, d.ebitda_trend], ["Net Income", d.net_income_3, d.net_income_2, d.net_income_1, d.net_income_trend]].map(([l, ...v]) => <tr key={l}>{[l, ...v].map((c, i) => <td key={i} style={{ padding: "5px 8px", borderBottom: "1px solid #1e293b", color: i === 0 ? "#94a3b8" : "#e2e8f0" }}>{c || "—"}</td>)}</tr>)}</tbody>
                </table>
              } />
              <Sec title="Cash Flow Statement" ch={
                <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                  <thead><tr>{["Item", d.year_3 || "Y-2", d.year_2 || "Y-1", d.year_1 || "Latest", "Trend"].map(h => <th key={h} style={{ background: "#1e3a5f", color: "#93c5fd", padding: "6px 8px", textAlign: "left" }}>{h}</th>)}</tr></thead>
                  <tbody>{[
                    ["Operating Activities", d.cash_flow_operating_3, d.cash_flow_operating_2, d.cash_flow_operating_1, d.cash_flow_operating_trend],
                    ["Investing Activities", d.cash_flow_investing_3, d.cash_flow_investing_2, d.cash_flow_investing_1, d.cash_flow_investing_trend],
                    ["Financing Activities", d.cash_flow_financing_3, d.cash_flow_financing_2, d.cash_flow_financing_1, d.cash_flow_financing_trend],
                    ["Cash at End of Period", d.cash_flow_end_3, d.cash_flow_end_2, d.cash_flow_end_1, d.cash_flow_end_trend],
                  ].map(([l, ...v]) => <tr key={l}>{[l, ...v].map((c, i) => <td key={i} style={{ padding: "5px 8px", borderBottom: "1px solid #1e293b", color: i === 0 ? "#94a3b8" : c && c !== "—" && c !== "null" ? "#e2e8f0" : "#475569" }}>{c || "—"}</td>)}</tr>)}</tbody>
                </table>
              } />
                <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                  <thead><tr>{["Item", d.year_3 || "Y-2", d.year_2 || "Y-1", d.year_1 || "Latest", "Trend"].map(h => <th key={h} style={{ background: "#1e3a5f", color: "#93c5fd", padding: "6px 8px", textAlign: "left" }}>{h}</th>)}</tr></thead>
                  <tbody>{[["Cash", d.cash_3, d.cash_2, d.cash_1, d.cash_trend], ["Accounts Rec.", d.ar_3, d.ar_2, d.ar_1, d.ar_trend], ["Total Assets", d.total_assets_3, d.total_assets_2, d.total_assets_1, d.total_assets_trend], ["Current Liab.", d.current_liabilities_3, d.current_liabilities_2, d.current_liabilities_1, d.current_liabilities_trend], ["Total Liab.", d.total_liabilities_3, d.total_liabilities_2, d.total_liabilities_1, d.total_liabilities_trend], ["Equity", d.equity_3, d.equity_2, d.equity_1, d.equity_trend]].map(([l, ...v]) => <tr key={l}>{[l, ...v].map((c, i) => <td key={i} style={{ padding: "5px 8px", borderBottom: "1px solid #1e293b", color: i === 0 ? "#94a3b8" : "#e2e8f0" }}>{c || "—"}</td>)}</tr>)}</tbody>
                </table>
              } />
            </div>}

            {/* RATIOS */}
            {tab === "ratios" && <div>
              {[["Liquidity", [["Current Ratio", d.current_ratio, d.current_ratio_industry, d.current_ratio_label, d.current_ratio_interpretation], ["Quick Ratio", d.quick_ratio, d.quick_ratio_industry, d.quick_ratio_label, d.quick_ratio_interpretation]]], ["Profitability", [["Gross Margin", `${d.gross_margin||"—"}%`, `${d.gross_margin_industry||"—"}%`, d.gross_margin_label, d.gross_margin_interpretation], ["EBITDA Margin", `${d.ebitda_margin||"—"}%`, `${d.ebitda_margin_industry||"—"}%`, d.ebitda_margin_label, d.ebitda_margin_interpretation], ["Net Margin", `${d.net_margin||"—"}%`, `${d.net_margin_industry||"—"}%`, d.net_margin_label, d.net_margin_interpretation]]], ["Leverage", [["Debt/Equity", d.debt_equity, d.debt_equity_industry, d.debt_equity_label, d.debt_equity_interpretation], ["Equity Ratio", `${d.equity_ratio||"—"}%`, `${d.equity_ratio_industry||"—"}%`, d.equity_ratio_label, d.equity_ratio_interpretation]]], ["Efficiency", [["Asset Turnover", d.asset_turnover, d.asset_turnover_industry, d.asset_turnover_label, d.asset_turnover_interpretation], ["DSO", `${d.dso||"—"}d`, `${d.dso_industry||"—"}d`, d.dso_label, d.dso_interpretation], ["DPO", `${d.dpo||"—"}d`, `${d.dpo_industry||"—"}d`, d.dpo_label, d.dpo_interpretation]]]].map(([title, rows]) => (
                <Sec key={title} title={`${title} Ratios`} ch={rows.map(([l, v, ind, lbl, interp]) => <div key={l} style={{ borderBottom: "1px solid #1e293b", padding: "8px 0" }}><div style={{ display: "flex", justifyContent: "space-between", fontSize: 13 }}><span style={{ color: "#64748b" }}>{l}</span><span style={{ color: "#e2e8f0", fontWeight: 600 }}>{v ?? "—"}</span></div><div style={{ fontSize: 12, color: "#475569", marginTop: 2 }}>Industry avg: {ind ?? "—"} · {lbl ?? "—"}</div>{interp && <div style={{ fontSize: 11, color: "#64748b", marginTop: 2 }}>{interp}</div>}</div>)} />
              ))}
            </div>}

            {/* RISK */}
            {tab === "risk" && <div>
              <Sec title="Credit Rating" ch={<>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
                  <div><div style={{ fontSize: 28, fontWeight: 800 }}>{d.credit_rating || "—"}</div><div style={{ color: "#475569", fontSize: 12 }}>Credit Rating</div></div>
                  <Badge v={d.risk_level} />
                </div>
                <Bar label="Health Score" val={d.health_score} />
                <Bar label="Viability Score" val={d.viability_score} />
                <Bar label="Payment Score" val={d.payment_score} />
                <Bar label="Delinquency Risk" val={d.delinquency_score} invert />
                <Bar label="Failure Risk" val={d.failure_score} invert />
                <F l="PAYDEX" v={d.paydex_score} /><F l="Company Size" v={d.company_size} /><F l="Annual Revenue" v={d.annual_revenue} />
              </>} />
              <Sec title="Payment Behavior" ch={<>
                <F l="Avg Days Beyond Terms" v={d.avg_dbt != null ? `${d.avg_dbt} days` : null} />
                <F l="% On Time" v={d.pct_on_time != null ? `${d.pct_on_time}%` : null} />
                <F l="Prompt" v={d.prompt_pct != null ? `${d.prompt_pct}% · ${d.prompt_amount}` : null} />
                <F l="1–30 Days Slow" v={d.slow_30_pct != null ? `${d.slow_30_pct}% · ${d.slow_30_amount}` : null} />
                <F l="90+ Days Slow" v={d.slow_90plus_pct != null ? `${d.slow_90plus_pct}% · ${d.slow_90plus_amount}` : null} />
              </>} />
              <Sec title="Alerts" ch={(d.alerts || []).map((a, i) => <div key={i} style={{ background: a.alert_type === "danger" ? "#450a0a" : a.alert_type === "warning" ? "#451a03" : a.alert_type === "success" ? "#052e16" : "#0c1a2e", borderRadius: 8, padding: "8px 12px", marginBottom: 6, fontSize: 13 }}>{a.alert_icon} {a.alert_message}</div>)} />
            </div>}

            {/* LEGAL */}
            {tab === "legal" && <div>
              <Sec title="Legal Summary" ch={[["Lawsuits", d.lawsuit_count, d.lawsuit_amount, d.lawsuit_status, d.lawsuit_badge], ["Liens", d.lien_count, d.lien_amount, d.lien_status, d.lien_badge], ["Judgments", d.judgment_count, d.judgment_amount, d.judgment_status, d.judgment_badge]].map(([t, c, a, s, b]) => <div key={t} style={{ borderBottom: "1px solid #1e293b", padding: "8px 0" }}><div style={{ display: "flex", justifyContent: "space-between", fontSize: 13 }}><span style={{ color: "#64748b" }}>{t}</span><span>Count: {c ?? "—"} · {a || "—"}</span></div><div style={{ fontSize: 12, color: "#475569" }}>Status: {s || "—"}</div></div>)} />
              <Sec title="Compliance" ch={<><F l="License" v={`${d.license_status || "—"} (exp: ${d.license_expiry || "—"})`} /><F l="Tax Status" v={d.tax_status} /></>} />
              {(d.legal_details || []).length > 0 && <Sec title="Legal Details" ch={(d.legal_details).map((l, i) => <div key={i} style={{ background: "#1e293b", borderRadius: 8, padding: 12, marginBottom: 8, fontSize: 13 }}><strong style={{ color: "#93c5fd" }}>{l.event_type} · {l.event_date}</strong><div style={{ color: "#94a3b8", marginTop: 4 }}>{l.event_description}</div></div>)} />}
            </div>}

            {/* NEWS */}
            {tab === "news" && <div>
              <Sec title="News & Events (2024+)" ch={!(d.news_events || []).length ? <div style={{ color: "#475569", fontSize: 13 }}>No 2024+ news found.</div> : (d.news_events).map((n, i) => <div key={i} style={{ background: "#1e293b", borderRadius: 8, padding: "10px 14px", marginBottom: 8, fontSize: 13 }}><div style={{ display: "flex", justifyContent: "space-between", marginBottom: 4 }}><span style={{ color: "#475569", fontSize: 11 }}>{n.event_date}</span><span style={{ background: n.event_sentiment === "positive" ? "#052e16" : n.event_sentiment === "negative" ? "#450a0a" : "#0c1a2e", color: "#e2e8f0", borderRadius: 4, padding: "1px 8px", fontSize: 11 }}>{n.event_sentiment_label}</span></div><strong style={{ color: "#93c5fd" }}>{n.event_title}</strong><div style={{ color: "#94a3b8", marginTop: 4 }}>{n.event_summary}</div></div>)} />
            </div>}

            {/* RECOMMENDATION */}
            {tab === "recommendation" && <div>
              <Sec title="Credit Decision" ch={<>
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
                  <div><div style={{ fontSize: 26, fontWeight: 800 }}>{d.final_credit_rating || "—"}</div><div style={{ color: "#475569", fontSize: 12 }}>Final Rating</div></div>
                  <Badge v={d.final_risk_level} />
                </div>
                <F l="Credit Limit (USD)" v={d.recommended_credit_limit} />
                <F l="Max Exposure (USD)" v={d.maximum_exposure} />
                <F l="Payment Terms" v={d.recommended_payment_terms} />
                <F l="Review Frequency" v={d.review_frequency} />
                {d.credit_opinion_text && <div style={{ background: "#1e293b", borderRadius: 8, padding: 14, marginTop: 12, fontSize: 13, color: "#cbd5e1", lineHeight: 1.7 }}><strong style={{ color: "#93c5fd", display: "block", marginBottom: 6 }}>📋 Credit Opinion</strong>{d.credit_opinion_text}</div>}
              </>} />
              <Sec title="Monitoring Triggers" ch={(d.monitoring_triggers || []).map((t, i) => <div key={i} style={{ borderBottom: "1px solid #1e293b", padding: "8px 0" }}><div style={{ color: "#fbbf24", fontSize: 13, fontWeight: 600 }}>⚠️ {t.trigger_event}</div><div style={{ color: "#94a3b8", fontSize: 12 }}>{t.trigger_action}</div></div>)} />
            </div>}

            {/* JSON */}
            {tab === "json" && <div style={{ background: "#0f172a", border: "1px solid #1e293b", borderRadius: 10, padding: 18 }}><pre style={{ color: "#86efac", fontSize: 11, overflowX: "auto", margin: 0, whiteSpace: "pre-wrap", wordBreak: "break-word" }}>{JSON.stringify(result, null, 2)}</pre></div>}

            {/* PATCH */}
            {tab === "patch" && (
              <div>
                <div style={{ background: "#0f172a", border: "1px solid #1e40af", borderRadius: 10, padding: 18, marginBottom: 14 }}>
                  <div style={{ color: "#93c5fd", fontSize: 12, fontWeight: 700, marginBottom: 10 }}>🔧 PASTE YOUR JSON</div>
                  <textarea
                    value={patchJSON}
                    onChange={e => setPatchJSON(e.target.value)}
                    placeholder='Paste your credit report JSON here...'
                    style={{ width: "100%", minHeight: 180, background: "#020817", border: "1px solid #1e293b", borderRadius: 8, color: "#86efac", fontSize: 11, padding: 12, fontFamily: "monospace", resize: "vertical", boxSizing: "border-box" }}
                  />
                  {result && <button onClick={() => setPatchJSON(JSON.stringify(result, null, 2))}
                    style={{ marginTop: 8, padding: "5px 12px", borderRadius: 6, border: "none", background: "#1e3a5f", color: "#93c5fd", cursor: "pointer", fontSize: 12 }}>
                    ↑ Load current report JSON
                  </button>}
                </div>
                <div style={{ background: "#0f172a", border: "1px solid #1e293b", borderRadius: 10, padding: 18, marginBottom: 14 }}>
                  <div style={{ color: "#fbbf24", fontSize: 12, fontWeight: 700, marginBottom: 10 }}>📋 PATCH INSTRUCTIONS</div>
                  <textarea
                    value={patchInstructions}
                    onChange={e => setPatchInstructions(e.target.value)}
                    placeholder={`Describe changes, e.g.:\n\n## WRONG VALUES — FIX THESE\n1. "tax_registration_number": "100..." → "100...3"\n2. "exec_current_ratio": "0.96" → "1.18"\n\n## EMPTY FIELDS — FILL\n3. "fax": "" → "N/A"\n4. "nace_codes": "" → "46.71"\n\n## ADD NEW FIELDS\n5. Add after "failure_color":\n   "failure_level": "Low",\n   "failure_probability": "8%"`}
                    style={{ width: "100%", minHeight: 240, background: "#020817", border: "1px solid #1e293b", borderRadius: 8, color: "#e2e8f0", fontSize: 12, padding: 12, fontFamily: "monospace", resize: "vertical", boxSizing: "border-box" }}
                  />
                </div>
                {patchStatus === "error" && <div style={{ background: "#1a0a0a", border: "1px solid #7f1d1d", borderRadius: 8, padding: 12, marginBottom: 12, color: "#fca5a5", fontSize: 13 }}>⚠️ {patchError}</div>}
                {patchStatus === "done" && <div style={{ background: "#052e16", border: "1px solid #16a34a", borderRadius: 8, padding: 12, marginBottom: 12, color: "#86efac", fontSize: 13 }}>✓ JSON patched! Switched to Raw JSON tab — download to save.</div>}
                <button onClick={runPatch} disabled={!patchJSON.trim() || !patchInstructions.trim() || patchStatus === "loading"}
                  style={{ width: "100%", padding: 13, borderRadius: 10, border: "none", background: !patchJSON.trim() || !patchInstructions.trim() || patchStatus === "loading" ? "#1e293b" : "linear-gradient(135deg,#f59e0b,#d97706)", color: !patchJSON.trim() || !patchInstructions.trim() || patchStatus === "loading" ? "#475569" : "#fff", fontWeight: 700, fontSize: 15, cursor: patchStatus === "loading" ? "default" : "pointer" }}>
                  {patchStatus === "loading" ? "Applying patches..." : "🔧 Apply Patches"}
                </button>
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}